// In JavaScript, everything is an object.

// Variables do not have any type, value do

// Coercion
const msg1 = "There are ";
const numStudents = 23;
const msg2 = " students.";

console.log(msg1 + numStudents + msg2);
